apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform-demo.fullname" . }}
  labels:
    {{- include "platform-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "platform-demo.selectorLabels" . | nindent 4 }}
